before_install:
  - if [ $TRAVIS_OS_NAME = linux ]; then sudo apt-get install tmux; else brew install tmux; fi

matrix:
  include:
    - os: osx
      osx_image: xcode8
    - os: linux

script:
  - make
  - make lint
  - make test
