before_install:
  - if [ $TRAVIS_OS_NAME = linux ]; then sudo apt-get update -yqqu && sudo apt-get install -yqq tmux; else brew install tmux; fi

matrix:
  include:
    - os: osx
      osx_image: xcode8
    - os: linux
      dist: trusty

script:
  - make
  - make lint
  - make test
